services:
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: ev-frontend
    container_name: ev-app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    tty: true

volumes:
  node_modules:
